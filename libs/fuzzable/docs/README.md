fuzzable
========

`fuzzable` is a small example library used to exercise fuzz harnesses. It intentionally provides a small API surface suitable for data-driven fuzzing.

Contents
- `include/fuzzable/`
- `src/`

This library is intentionally minimal and should be used as a template for adding new fuzzable components.
