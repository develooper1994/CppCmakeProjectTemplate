Dummy Lib
=========

This is a tiny example library used for template and test purposes. It is intentionally minimal and is not intended for production use.

Contents

- `include/dummy_lib/` — public headers
- `src/` — implementation

Use `python3 scripts/tool.py lib info dummy_lib` to see generated metadata.
