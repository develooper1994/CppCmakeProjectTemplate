// cmake/FeatureFlags.h.in — AUTO-GENERATED, DO NOT EDIT
// Regenerated every CMake configure run.
// Reflects the exact options passed to this build.
#pragma once

// ── Build type ────────────────────────────────────────────────────────────────
// BUILD_SHARED_LIBS
#cmakedefine01 PROJECT_SHARED_LIBS
#define PROJECT_LIBRARY_TYPE "@PROJECT_FEATURE_LIBRARY_TYPE@"

// ── Tests ─────────────────────────────────────────────────────────────────────
// ENABLE_UNIT_TESTS
#cmakedefine01 FEATURE_UNIT_TESTS
// ENABLE_GTEST
#cmakedefine01 FEATURE_GTEST
// ENABLE_CATCH2
#cmakedefine01 FEATURE_CATCH2
// ENABLE_BOOST_TEST
#cmakedefine01 FEATURE_BOOST_TEST
// ENABLE_QT → QTest
#cmakedefine01 FEATURE_QTEST

// ── Sanitizers ────────────────────────────────────────────────────────────────
// ENABLE_ASAN
#cmakedefine01 FEATURE_ASAN
// ENABLE_UBSAN
#cmakedefine01 FEATURE_UBSAN
// ENABLE_TSAN
#cmakedefine01 FEATURE_TSAN

// ── Static analysis ───────────────────────────────────────────────────────────
// ENABLE_CLANG_TIDY
#cmakedefine01 FEATURE_CLANG_TIDY
// ENABLE_CPPCHECK
#cmakedefine01 FEATURE_CPPCHECK

// ── Coverage ──────────────────────────────────────────────────────────────────
// ENABLE_COVERAGE
#cmakedefine01 FEATURE_COVERAGE

// ── Optional frameworks ───────────────────────────────────────────────────────
// ENABLE_QT
#cmakedefine01 FEATURE_QT
// ENABLE_QML
#cmakedefine01 FEATURE_QML
// ENABLE_BOOST
#cmakedefine01 FEATURE_BOOST

// ── Docs ──────────────────────────────────────────────────────────────────────
// ENABLE_DOCS
#cmakedefine01 FEATURE_DOCS

// ── Runtime inspection ───────────────────────────────────────────────────────
#include <string_view>
#include <array>

namespace project_features {

struct Feature {
    std::string_view name;
    bool             enabled;
};

// All features as a compile-time array — iterate to log/dump at startup.
inline constexpr std::array features = {
    Feature{"shared_libs",  bool(PROJECT_SHARED_LIBS)},
    Feature{"unit_tests",   bool(FEATURE_UNIT_TESTS)},
    Feature{"gtest",        bool(FEATURE_GTEST)},
    Feature{"catch2",       bool(FEATURE_CATCH2)},
    Feature{"boost_test",   bool(FEATURE_BOOST_TEST)},
    Feature{"qtest",        bool(FEATURE_QTEST)},
    Feature{"asan",         bool(FEATURE_ASAN)},
    Feature{"ubsan",        bool(FEATURE_UBSAN)},
    Feature{"tsan",         bool(FEATURE_TSAN)},
    Feature{"clang_tidy",   bool(FEATURE_CLANG_TIDY)},
    Feature{"cppcheck",     bool(FEATURE_CPPCHECK)},
    Feature{"coverage",     bool(FEATURE_COVERAGE)},
    Feature{"qt",           bool(FEATURE_QT)},
    Feature{"qml",          bool(FEATURE_QML)},
    Feature{"boost",        bool(FEATURE_BOOST)},
    Feature{"docs",         bool(FEATURE_DOCS)},
};

} // namespace project_features
