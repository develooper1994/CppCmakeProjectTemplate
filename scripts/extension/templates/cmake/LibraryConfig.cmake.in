@PACKAGE_INIT@

# @export_name@Config.cmake.in
# Bu dosya install_project_library() tarafından configure_package_config_file() ile işlenir.
# find_package(@export_name@) çağrısı bu dosyayı bulur.

include(CMakeFindDependencyMacro)

# Downstream projenin ihtiyaç duyduğu bağımlılıkları buraya ekle:
# find_dependency(SomeLib REQUIRED)

include("${CMAKE_CURRENT_LIST_DIR}/@export_name@Targets.cmake")

check_required_components(@export_name@)
