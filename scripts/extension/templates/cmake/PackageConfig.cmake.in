# cmake/PackageConfig.cmake.in
# Generic CMake package config template used by install_project_library().
# configure_package_config_file() replaces @PACKAGE_INIT@ at generation time.

@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/@export_name@Targets.cmake")

check_required_components(@export_name@)
