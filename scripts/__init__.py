"""Package marker for the `scripts` directory.

This file makes `scripts` a proper Python package so modules under it
can be executed with `python -m scripts.<module>` (for example
`python -m scripts.tui`).

The file is intentionally minimal.
"""

__all__ = []
